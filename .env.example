# Supabase PostgreSQL connection strings.
DATABASE_URL=""
DIRECT_URL=""

# Supabase project settings for auth and storage.
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""

# Public origin, used for the sitemap, robots policy, and RSS feed.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# The one address allowed to reach the admin studio. Required — the app refuses
# to start without it. The password is NOT configured here: it lives in Supabase
# Auth, which is the only thing that verifies it.
ADMIN_EMAIL=""
